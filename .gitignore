node_modules/
dist/
.vite/

# SQLite database files — committed intentionally for the demo so the
# first deploy on Vercel renders with real data. Writes are ephemeral
# on serverless; storage.ts re-seeds on cold start.
data.db-shm
data.db-wal
data.db-journal

.env
.env.*
!.env.example

*.log
.DS_Store
.vercel
